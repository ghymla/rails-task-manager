<br>
<br>

<h1> <%= @task.title %> </h1>
<p><%= @task.details %></p><i><%= @task.completed %></i>
<p><%= link_to "destroy task", task_path(@task.id), method: :delete, data: {confirm: "realy ?" } %><p>
<p><%= link_to "All Tasks", tasks_path%> |-| <%= link_to "update task", edit_task_path(@task.id) %></p>
